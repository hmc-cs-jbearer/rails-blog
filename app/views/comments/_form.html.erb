<%= form_for([@article, @article.comments.build]) do |f| %>
  <div class="form-group">
    <%= f.text_area :body, class: 'form-control', placeholder: 'Add a comment...' %>
  </div>

  <%= f.submit "Comment as " + current_user.email, class: "btn btn-default" %>
<% end %>
